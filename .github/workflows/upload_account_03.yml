# Auto-generated by 10_performance_scorer.py - DO NOT EDIT MANUALLY
# アカウント03: ライフハックチャンネル
# 投稿頻度: 週2本（Monday, Thursday）
name: Upload - account_03 (ライフハックチャンネル)

on:
  schedule:
    - cron: "0 14 * * 1"  # Monday
    - cron: "0 14 * * 4"  # Thursday
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Dry run（投稿スキップ）"
        type: boolean
        default: false

jobs:
  upload:
    uses: ./.github/workflows/upload_account.yml
    with:
      account_id: "account_03"
      dry_run: ${{ github.event_name == 'workflow_dispatch' && inputs.dry_run || false }}
    secrets:
      YOUTUBE_REFRESH_TOKEN: ${{ secrets.YOUTUBE_REFRESH_TOKEN_ACCT03 }}
      YOUTUBE_CLIENT_ID: ${{ secrets.YOUTUBE_CLIENT_ID_ACCT03 }}
      YOUTUBE_CLIENT_SECRET: ${{ secrets.YOUTUBE_CLIENT_SECRET_ACCT03 }}
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
      GCP_CREDENTIALS_JSON: ${{ secrets.GCP_CREDENTIALS_JSON }}
      PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
      PIXABAY_API_KEY: ${{ secrets.PIXABAY_API_KEY }}
