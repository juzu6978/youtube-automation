# Auto-generated by 10_performance_scorer.py - DO NOT EDIT MANUALLY
# アカウント05: お金・投資入門チャンネル
# 投稿頻度: 週2本（Tuesday, Friday）
name: Upload - account_05 (お金・投資入門チャンネル)

on:
  schedule:
    - cron: "0 14 * * 2"  # Tuesday
    - cron: "0 14 * * 5"  # Friday
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Dry run（投稿スキップ）"
        type: boolean
        default: false

jobs:
  upload:
    uses: ./.github/workflows/upload_account.yml
    with:
      account_id: "account_05"
      dry_run: ${{ github.event_name == 'workflow_dispatch' && inputs.dry_run || false }}
    secrets:
      YOUTUBE_REFRESH_TOKEN: ${{ secrets.YOUTUBE_REFRESH_TOKEN_ACCT05 }}
      YOUTUBE_CLIENT_ID: ${{ secrets.YOUTUBE_CLIENT_ID_ACCT05 }}
      YOUTUBE_CLIENT_SECRET: ${{ secrets.YOUTUBE_CLIENT_SECRET_ACCT05 }}
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
      GCP_CREDENTIALS_JSON: ${{ secrets.GCP_CREDENTIALS_JSON }}
      PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
      PIXABAY_API_KEY: ${{ secrets.PIXABAY_API_KEY }}
