# Auto-generated by 10_performance_scorer.py - DO NOT EDIT MANUALLY
# アカウント04: 歴史・偉人解説チャンネル
# 投稿頻度: 週2本（Wednesday, Sunday）
name: Upload - account_04 (歴史・偉人解説チャンネル)

on:
  schedule:
    - cron: "0 14 * * 3"  # Wednesday
    - cron: "0 14 * * 0"  # Sunday
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Dry run（投稿スキップ）"
        type: boolean
        default: false

jobs:
  upload:
    uses: ./.github/workflows/upload_account.yml
    with:
      account_id: "account_04"
      dry_run: ${{ github.event_name == 'workflow_dispatch' && inputs.dry_run || false }}
    secrets:
      YOUTUBE_REFRESH_TOKEN: ${{ secrets.YOUTUBE_REFRESH_TOKEN_ACCT04 }}
      YOUTUBE_CLIENT_ID: ${{ secrets.YOUTUBE_CLIENT_ID_ACCT04 }}
      YOUTUBE_CLIENT_SECRET: ${{ secrets.YOUTUBE_CLIENT_SECRET_ACCT04 }}
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
      GCP_CREDENTIALS_JSON: ${{ secrets.GCP_CREDENTIALS_JSON }}
      PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
      PIXABAY_API_KEY: ${{ secrets.PIXABAY_API_KEY }}
